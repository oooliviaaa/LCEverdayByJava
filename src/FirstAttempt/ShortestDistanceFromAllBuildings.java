package FirstAttempt;

public class ShortestDistanceFromAllBuildings {

	/**
	 * A BFS problem. 
	 * 
	 * Search from each building and calculate the distance to the building. 
	 * One thing to note is an empty land must be reachable by all buildings. 
	 * To achieve this, maintain an array of counters. 
	 * Each time we reach a empty land from a building, increase the counter. 
	 * Finally, a reachable point must have the counter equaling to the number of buildings. 
	 * 
	 * 从每个building出发进行BFS
	 * */
	public int shortestDistance(int[][] grid) {
        
    }
}
